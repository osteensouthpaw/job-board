scrape_configs:
  - job_name: "jobMega"
    metrics_path: "/actuator/prometheus"
    scrape_interval: 3s # how often to scrape
    static_configs:
      - targets: [ "${SPRING_APP_HOST}:${SPRING_APP_PORT}" ]

